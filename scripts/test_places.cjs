const {test}=require('node:test'); const assert=require('node:assert/strict');
const Places=require('../frontend/places.js');
const memory=(initial={})=>{const m=new Map(Object.entries(initial));return {getItem:k=>m.has(k)?m.get(k):null,setItem:(k,v)=>m.set(k,String(v)),removeItem:k=>m.delete(k),raw:m}};
test('migrates the legacy province once without inventing an exact location',()=>{const s=memory({straznik_voiv:'lubelskie'});const p=Places.migrate(s);assert.deepEqual(p[0],{id:'legacy-home',name:'Dom',precision:'region',region:'lubelskie',gps:null,alerts:true});});
test('deduplicates watched provinces and respects opt-out',()=>{const p=[{region:'lubelskie',alerts:true},{region:'lubelskie',alerts:true},{region:'pomorskie',alerts:false},{region:'podlaskie',alerts:true}];assert.deepEqual(Places.observedVoivodeships(p),['lubelskie','podlaskie']);});
test('sanitizes local profiles and limits their count',()=>{const s=memory();const p=Places.save(s,Array.from({length:10},(_,i)=>({id:i,name:' x '.repeat(30),precision:'region',region:'mazowieckie',alerts:true})));assert.equal(p.length,8);assert.equal(p[0].name.length,40);assert.equal(p[0].gps,null);});
test('keeps one-time GPS only for GPS precision',()=>{const a=Places.clean({name:'P',precision:'gps',region:'lubelskie',gps:{lat:51.2,lon:22.5,accuracy:42,capturedAt:'2026-09-09T12:00:00Z'}});assert.equal(a.gps.accuracy,42);const b=Places.clean({...a,precision:'region'});assert.equal(b.gps,null);});
test('calculates exact-point distance locally and ETA only on a matching course',()=>{const place=Places.clean({name:'Dom',precision:'gps',region:'lubelskie',gps:{lat:52,lon:22,accuracy:10}});const toward=Places.exactPoint(place,{lat:51,lon:22,heading:0},600);assert.ok(toward.distanceKm>111&&toward.distanceKm<112);assert.equal(toward.etaMin,8);assert.equal(Places.exactPoint(place,{lat:51,lon:22,heading:180},600).reason,'course');});
test('does not invent an exact-point ETA without heading or measured speed',()=>{const place={gps:{lat:52,lon:22}};assert.equal(Places.exactPoint(place,{lat:51,lon:22},600).reason,'heading');assert.equal(Places.exactPoint(place,{lat:51,lon:22,heading:0},null).reason,'speed');});
test('rejects missing and out-of-range coordinates',()=>{assert.equal(Places.clean({name:'P',precision:'gps',region:'lubelskie',gps:{lat:null,lon:null}}).gps,null);assert.equal(Places.exactPoint({gps:{lat:52,lon:22}},{lat:null,lon:null,heading:0},600),null);assert.equal(Places.clean({name:'P',precision:'gps',region:'lubelskie',gps:{lat:95,lon:22}}).gps,null);});
